## .NET build output
bin/
obj/
dist/
publish/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
.vs/

## Test / coverage
artifacts/
[Tt]est[Rr]esults/
*.trx
coverage*.json
coverage*.xml

## Rider / VS Code
.idea/
.vscode/

## Local machine config (never committed — §4)
machines.json

# Copias locales para diagn�stico forense (F3.1 Bloque 1)
diagnostics/

## Worktrees del agente (R1). `git worktree add .claude/worktrees/<x>` deja un fichero
## `.git` suelto en la raiz del arbol principal, que se veia como cambio sin seguir.
.claude/worktrees/
