<UserControl
    x:Class="Atalaya.App.Views.SessionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="280" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Sesión en vivo" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Text="{Binding HeaderText}" Opacity="0.6" VerticalAlignment="Bottom" Margin="12,0,0,4" />
        </StackPanel>

        <!-- Unit queue -->
        <Border Grid.Row="1" Grid.Column="0" Background="#14808080" CornerRadius="8" Padding="10" Margin="0,0,10,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Queue}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Path}" TextTrimming="CharacterEllipsis" />
                                <Border Grid.Column="1" Background="#22808080" CornerRadius="8" Padding="6,1" Margin="6,0,0,0">
                                    <TextBlock Text="{Binding Phase}" FontSize="10" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Streamed agent text -->
        <Border Grid.Row="1" Grid.Column="1" Background="#0C000000" CornerRadius="8" Padding="12" Margin="0,0,10,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding Streamed}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
            </ScrollViewer>
        </Border>

        <!-- Findings + telemetry -->
        <Border Grid.Row="1" Grid.Column="2" Background="#14808080" CornerRadius="8" Padding="10">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Hallazgos" FontWeight="SemiBold" Margin="0,0,0,8" />
                <StackPanel DockPanel.Dock="Bottom" Margin="0,8,0,0">
                    <TextBlock><Run Text="Tokens in: " /><Run Text="{Binding InputTokens}" FontWeight="Bold" /></TextBlock>
                    <TextBlock><Run Text="Tokens out: " /><Run Text="{Binding OutputTokens}" FontWeight="Bold" /></TextBlock>
                    <TextBlock>
                        <Run Text="Coste: " /><Run Text="{Binding Cost}" FontWeight="Bold" />
                        <Run Text=" " /><Run Text="{Binding CostUnit}" Foreground="#99FFFFFF" />
                    </TextBlock>
                </StackPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Findings}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,2" FontSize="12" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- Controls -->
        <Grid Grid.Row="2" Grid.ColumnSpan="3" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal">
                <ui:Button Content="Detener" Command="{Binding StopCommand}" Appearance="Danger"
                           Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibility}}" />
                <ui:ProgressRing IsIndeterminate="True" Width="18" Height="18" Margin="12,0,0,0"
                                 Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibility}}" />
            </StackPanel>
            <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Right" VerticalAlignment="Center"
                       Opacity="0.85" TextWrapping="Wrap" MaxWidth="700"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NotEmptyToVisibility}}" />
        </Grid>
    </Grid>
</UserControl>
