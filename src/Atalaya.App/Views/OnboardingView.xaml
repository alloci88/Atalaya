<UserControl
    x:Class="Atalaya.App.Views.OnboardingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="620" HorizontalAlignment="Left">
            <TextBlock Text="Nueva aplicación" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,6" />
            <TextBlock Text="Registra un repo auditable: detecta el stack y construye el inventario del primer ciclo."
                       Opacity="0.7" TextWrapping="Wrap" Margin="0,0,0,16" />

            <TextBlock Text="Nombre" FontWeight="SemiBold" Margin="0,4,0,4" />
            <ui:TextBox Text="{Binding Name}" Margin="0,0,0,12" />

            <TextBlock Text="URL del repo de la aplicación" FontWeight="SemiBold" Margin="0,4,0,4" />
            <ui:TextBox Text="{Binding RepoUrl}" Margin="0,0,0,12" />

            <TextBlock Text="Ruta del clon local (en esta máquina)" FontWeight="SemiBold" Margin="0,4,0,4" />
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:TextBox Grid.Column="0" Text="{Binding ClonePath}" />
                <ui:Button Grid.Column="1" Content="Detectar stack" Command="{Binding DetectCommand}" Margin="8,0,0,0" />
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                <TextBlock Text="Stack detectado: " Opacity="0.7" />
                <TextBlock Text="{Binding DetectedStack}" FontWeight="SemiBold" />
            </StackPanel>

            <ui:Button Content="Crear e inventariar" Command="{Binding CreateCommand}" Appearance="Primary" HorizontalAlignment="Left" />
            <TextBlock Text="{Binding StatusMessage}" Opacity="0.85" TextWrapping="Wrap" Margin="0,12,0,0"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NotEmptyToVisibility}}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
